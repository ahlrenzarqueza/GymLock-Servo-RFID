/**
 * 定义 token 类
 */
export declare class Token {
    value: any;
    type: number;
    static TYPE_NORMAL: number;
    static TYPE_OPTION_NAME: number;
    static TYPE_OPTION_VALUE: number;
    /**
     * 构造一个 token
     * @param value 值
     * @param type 类别
     */
    constructor(value: any, type: number);
    /**
     * 解析 token
     * - 以「-」开头的字符串视为「选项名」
     * - 以「-」开头并且包含「＝」，将拆分为「选项名」和「选项值」
     * - 其它的都视为「普通字符」
     * @param strArray 字符串列表
     */
    static parse(strArray: string[]): TokenList;
}
export declare class TokenList extends Array<Token> {
}
